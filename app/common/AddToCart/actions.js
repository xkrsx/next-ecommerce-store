// 'use server';

// import { cookies } from 'next/headers';

// export async function createOrUpdateCookie(productId, count) {
//   await cookies().set(
//     'cart',
//     JSON.stringify([{ productId: productId, count: count }]),
//   );
// }
